version: "3.9"
services:
  web:
    build: ./
    container_name: network
    restart: always
    command: bash entrypoint.sh
    volumes:
      - .:/usr/src/app
      # - ./static: /usr/src/app/static
      # - ./media: /usr/src/app/media
    ports:
      - "8000:8000"